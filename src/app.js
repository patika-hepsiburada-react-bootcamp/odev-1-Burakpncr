import {getData} from './lib/service.js' // sevice js'den fonksyonumu import ettim
console.log(await getData(1)) // burada yukarıda import ettiğim fonksyon ile istenilen parametreye uygun Data'lar getirilebildi.
